{
  "name": "@sotaoi/strider",
  "private": false,
  "version": "0.9.1",
  "description": "",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "bootstrap": "npm install --no-audit --no-funds",
    "bootstrap:prod": "npm install --production --no-audit --no-funds",
    "deploy:sstrider": "node ./scripts/deploy/deploy-sstrider",
    "clean:bootstrap": "node ./scripts/clean/clean-bootstrap",
    "clean:sstrider": "node ./scripts/clean/clean-sstrider"
  },
  "dependencies": {
    "@sotaoi/omni": "git+https://github.com/sotaoi/omni#0.9.1"
  }
}
